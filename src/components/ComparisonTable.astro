---
interface Device {
  name: string;
  [key: string]: string;
}

interface Props {
  devices: Device[];
  specs: { key: string; label: string }[];
  title?: string;
}

const { devices, specs, title = 'Geräte-Vergleich' } = Astro.props;
---

<div class="not-prose my-8 overflow-x-auto rounded-xl border border-stone-200 shadow-sm">
  {title && (
    <div class="bg-ink-900 px-4 py-3">
      <h3 class="text-sm font-semibold text-white">{title}</h3>
    </div>
  )}
  <table class="w-full text-sm">
    <thead>
      <tr class="border-b border-stone-200">
        <th class="bg-stone-50 px-4 py-3 text-left text-xs font-semibold text-stone-500 uppercase tracking-wide w-32">
          Eigenschaft
        </th>
        {devices.map(device => (
          <th class="bg-stone-50 px-4 py-3 text-left text-xs font-semibold text-ink-900">
            {device.name}
          </th>
        ))}
      </tr>
    </thead>
    <tbody>
      {specs.map((spec, i) => (
        <tr class:list={['border-b border-stone-100 last:border-0', i % 2 === 1 ? 'bg-stone-50/50' : 'bg-white']}>
          <td class="px-4 py-3 text-xs font-medium text-stone-500">{spec.label}</td>
          {devices.map(device => (
            <td class="px-4 py-3 text-stone-800">{device[spec.key] ?? '–'}</td>
          ))}
        </tr>
      ))}
    </tbody>
  </table>
</div>
